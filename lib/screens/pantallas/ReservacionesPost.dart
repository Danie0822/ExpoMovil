import 'package:flutter/material.dart';

class ReservacioneScreen extends StatelessWidget {
   
  const ReservacioneScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('ReservacioneScreen'),
      ),
    );
  }
}